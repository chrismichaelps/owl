{
  "contract_id": "CTR-tui-app-00000001",
  "module": "@Owl.TUI.App src/tui/app.tsx",
  "grammar_lock": "@root/hashes/grammar/react/react.hash.md",
  "depth_score": "0.82 — DEEP (main layout shell)",
  "seam_capacity": "BACKBONE (connects all TUI components)",
  "sig_id": "SIG-tui-app-89b01fb3",
  "interface": {
    "signatures": [
      "OwlApp(props: OwlAppProps) => ReactElement",
      "AppProps.initialPermissionMode?: ToolPermissionMode",
      "AppProps.initialPrivacyMode?: boolean",
      "AppProps.initialResumeSessionId?: string | null",
      "AppProps.initialProviderOverride?: ProviderId | null",
      "App passes OwlAppState.executionStage to LogPanel, MetaPanel, and StatusBar"
    ],
    "error_modes": [],
    "invariants": [
      "Startup Permission mode MUST be applied before optional initial prompt execution",
      "Startup Resume Session id MUST be applied before optional initial prompt execution",
      "Startup Privacy mode MUST be applied before startup Provider override",
      "Startup Provider override MUST be applied before optional initial prompt execution",
      "Startup Provider override MUST route through CommandRegistry /model dispatch",
      "Execution stage state MUST be rendered without duplicating lifecycle logic inside presentation components."
    ]
  },
  "responsibilities": [
    "Route Metrics-panel pending Mutation shortcuts through CommandRegistry dispatch",
    "Initialize visible and runtime Permission mode from CLI startup args",
    "Initialize SessionMemory from CLI startup Resume Session id",
    "Initialize visible and runtime Privacy mode from CLI startup args",
    "Initialize visible and runtime Provider override from CLI startup args",
    "Surface visible execution stage for runtime observability"
  ],
  "prohibited": [
    "MUST NOT resolve pending Mutation approval shortcuts inline; use @root/src/tui/pending/shortcuts.ts"
  ],
  "created_at": "2026-05-14T15:55:47.198530Z"
}
