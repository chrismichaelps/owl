{
  "contract_id": "CTR-tui-pending-shortcuts-00000001",
  "module": "@Owl.TUI.Pending.Shortcuts src/tui/pending/shortcuts.ts",
  "grammar_lock": "@root/hashes/grammar/react/react.hash.md",
  "depth_score": "0.70 — DEEP (keyboard approval policy behind one resolver)",
  "seam_capacity": "INTERNAL",
  "sig_id": "SIG-tui-pending-shortcuts-00000001",
  "interface": {
    "signatures": [
      "resolvePendingApprovalShortcut(input: string, focusedPanel: string, pendingMutationIds: Chunk<string>) => Option<PendingApprovalShortcut>"
    ],
    "error_modes": [],
    "invariants": [
      "Shortcuts MUST only resolve while the Metrics panel is focused",
      "Shortcuts MUST only resolve when at least one pending Mutation exists",
      "Shortcuts MUST return slash commands accepted by the CommandRegistry",
      "Shortcuts MUST NOT inspect PendingMutationStore directly"
    ]
  },
  "responsibilities": [
    "Map focused-panel keystrokes to pending Mutation approval commands",
    "Keep App input handling free of approval policy details",
    "Prevent approval shortcuts from hijacking normal prompt typing"
  ],
  "prohibited": [
    "MUST NOT call runtime services",
    "MUST NOT mutate TUI state",
    "MUST NOT parse command arguments",
    "MUST NOT use native arrays for ordered pending Mutation IDs"
  ],
  "dependencies": [
    "@root/hashes/src/core/constants/tui.hash.md"
  ],
  "domain_refs": [
    "docs/CONTEXT.md#Mutation",
    "docs/CONTEXT.md#MutationPreview"
  ],
  "variants": []
}
