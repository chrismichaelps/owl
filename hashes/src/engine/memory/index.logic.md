# Logic Blueprint: @Owl.Engine.Memory src/engine/memory/index.ts

## State Machine

| State       | Transition    | Guard                  |
| ----------- | ------------- | ---------------------- |
| MEM_EMPTY   | loadContext   | session.exists         |
| MEM_LOADING | contextLoaded | context.length > 0     |
| MEM_READY   | addMessage    | message.valid          |
| MEM_READY   | prune         | tokenCount > maxTokens |
| MEM_FULL    | evict         | oldest.ts < threshold  |

## Algorithm

1. **Initialize** — Set up active Session state and validate preconditions.
2. **Start Session** — Create an empty active Session with either a supplied id or the next deterministic generated id.
3. **Resume Session** — Switch active Session while preserving stored turns.
4. **Record Turn** — Decode and append turns to the active Session, bounded by retention constants.
5. **List Sessions** — Return known Session ids in deterministic lexical order.
6. **Complete** — Finalize operation and clean up resources.

## Negative Logic (PROHIBITED PATHS)

- MUST NOT: Bypass state transitions — always use the defined state machine
- MUST NOT: Skip guard validation — every transition requires guard check
- MUST NOT: Mutate state directly — use only defined transition methods
- MUST NOT: expose the mutable Session HashMap to callers.

## Edge Cases

- **Concurrent access**: Serialize state transitions via atomic operations
- **Timeout during transition**: Roll back to previous valid state
- **Invalid guard condition**: Log error and remain in current state

## Dependencies

- Grammar: "@root/hashes/grammar/effect/effect.hash.md"
- Parent: `@root/hashes/src/engine/orchestrator/index.hash.md`

---

_Generated by FMCF Forensic Guardian — 2026-05-14T15:58:03.924965Z_

## Multi-Session Persistence

- toPersistedState serializes every known Session; fromPersistedState restores them into HashMap-backed runtime state; generated Session counters resume after the highest persisted generated id.

## Resume Semantics

- resumeSession(sessionId) switches to existing turns when present, otherwise creates an empty Session for that id.
