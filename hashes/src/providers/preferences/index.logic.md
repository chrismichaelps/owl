# Logic Blueprint: @Owl.Providers.Preferences src/providers/preferences/index.ts

## Algorithm

1. Initialize a Ref containing an empty RoutingPreferencesState.
2. On `setPreferredProvider`, replace the state with the schema-validated ProviderId.
3. On `clearPreferredProvider`, replace the state with an empty state.
4. On `getPreferredProvider`, return the active ProviderId or undefined.
5. On `snapshot`, return the complete immutable RoutingPreferencesState for Command display.

## Negative Logic (PROHIBITED PATHS)

- MUST NOT: use module-level mutable variables for RoutingPreference state.
- MUST NOT: store unvalidated Provider strings after Command parsing.
- MUST NOT: couple RoutingPreferences to concrete Provider adapters.
- MUST NOT: disable ProviderRouter fallback when a RoutingPreference is active.

## Edge Cases

- **No active RoutingPreference**: Orchestrator omits `preferredProvider` so ProviderRouter uses scoring.
- **Preferred Provider unavailable**: ProviderRouter failover remains responsible for recovery.
- **Command clears preference**: Next Inference returns to automatic ProviderRouter selection.

## Dependencies

- Grammar: "@root/hashes/grammar/effect/effect.hash.md"
- Domain: `docs/CONTEXT.md#RoutingPreference`
- Parent: `@root/hashes/src/providers/router/index.hash.md`

---

*Generated by FMCF DNA Engineer — 2026-05-14T16:55:00.000000Z*
