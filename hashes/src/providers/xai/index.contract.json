{
  "contract_id": "CTR-providers-xai-00000001",
  "module": "@Owl.Providers.xAI src/providers/xai/index.ts",
  "grammar_lock": "@root/hashes/grammar/typescript/typescript.hash.md",
  "depth_score": "0.72 \u2014 DEEP (OpenAI-compatible integration)",
  "seam_capacity": "CRITICAL",
  "sig_id": "SIG-providers-xai-00000001",
  "interface": {
    "signatures": [
      "XAI_CAPABILITIES",
      "XAIAdapter extends Context.Tag",
      "XAIAdapterLive: Layer"
    ],
    "error_modes": [
      "ProviderError when XAI_API_KEY is not configured",
      "ProviderError when xAI SDK-compatible request fails"
    ],
    "invariants": [
      "XAIAdapterLive MUST construct successfully without XAI_API_KEY",
      "XAIAdapterLive MUST NOT instantiate the OpenAI-compatible SDK client without XAI_API_KEY",
      "Unconfigured xAI operations MUST fail with tagged ProviderError"
    ]
  },
  "responsibilities": [
    "Expose xAI Provider capabilities to runtime bootstrap",
    "Execute xAI Inference when configured",
    "Remain inert and typed when configuration is absent"
  ],
  "prohibited": [
    "MUST NOT throw during Layer construction when XAI_API_KEY is missing",
    "MUST NOT allow optional Provider configuration to block runtime startup"
  ],
  "created_at": "2026-05-14T15:55:47.203029Z"
}
