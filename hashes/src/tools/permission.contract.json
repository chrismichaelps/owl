{
  "contract_id": "CTR-tools-permission-3552f24b",
  "module": "@Owl.Tools.Permission src/tools/permission.ts",
  "grammar_lock": "@root/hashes/grammar/effect/effect.hash.md",
  "depth_score": "0.76 — DEEP (permission mode policy behind pure resolver)",
  "seam_capacity": "CRITICAL",
  "sig_id": "SIG-tools-permission-3552f24b",
  "interface": {
    "signatures": [
      "resolveToolPermission(risk: ToolRiskAssessment, mode?: ToolPermissionMode) => ToolPermissionDecision",
      "formatToolPermission(decision: ToolPermissionDecision) => string"
    ],
    "error_modes": [],
    "invariants": [
      "Permission resolution MUST be pure and deterministic",
      "Permission resolution MUST consume ToolRisk instead of reclassifying tool input",
      "BLOCKED ToolRisk MUST always resolve to deny",
      "Permission modes MUST match the registered external Permission vocabulary",
      "Permission decisions MUST be represented as Effect-hashable domain data"
    ]
  },
  "responsibilities": [
    "Resolve allow, ask, or deny behavior from ToolRisk and permission Mode",
    "Expose registered Permission mode vocabulary",
    "Provide a visible policy seam for tool commands before interactive prompts exist"
  ],
  "prohibited": [
    "MUST NOT execute tools",
    "MUST NOT prompt the user directly",
    "MUST NOT inspect the file system",
    "MUST NOT use native objects for domain decision construction",
    "MUST NOT allow BLOCKED ToolRisk in any permission Mode"
  ],
  "dependencies": [
    "@root/hashes/src/tools/risk.contract.json",
    "@root/hashes/src/core/constants/tools.hash.md"
  ],
  "domain_refs": ["docs/CONTEXT.md#Permission", "docs/CONTEXT.md#ToolRisk"],
  "variants": []
}
