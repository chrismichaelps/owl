# Logic Blueprint: @Owl.Entry (src/main.ts)

## State Machine

| State | Transition | Guard |
|-------|------------|-------|
| BOOT_IDLE | startMain | runtime.ready |
| BOOT_INITIALIZING | initServices | services.loaded |
| BOOT_READY | enterREPL | cli.active |
| BOOT_ERROR | handleBootError | error != null |

## Algorithm

1. **Initialize** — Set up Effect runtime and services
2. **Load** — Bootstrap all required layers
3. **Execute** — Run the main CLI loop
4. **Shutdown** — Clean up resources on exit

## Negative Logic (PROHIBITED PATHS)

- MUST NOT: Put business logic in entry point
- MUST NOT: Use console.log directly — use logging layer

## Edge Cases

- **Missing config**: Fail gracefully with clear error message
- **Runtime panic**: Catch all errors and log before exit

## Dependencies

- Grammar: @root/hashes/grammar/typescript/typescript.hash.md
- Parent: @root/hashes/local.map.json

---

*Generated by FMCF Forensic Guardian — 2026-05-14T16:00:00Z*