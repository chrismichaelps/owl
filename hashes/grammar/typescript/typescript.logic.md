# Logic Blueprint: Grammar Shard — TypeScript 5.x

## State Machine

| State | Transition | Guard |
|-------|------------|-------|
| GRAMMAR_IDLE | loadShard | grammar.valid |
| GRAMMAR_ACTIVE | applyPattern | pattern.anchored |
| GRAMMAR_VERIFIED | syntaxValid | errors.length = 0 |
| GRAMMAR_DRIFTED | detectDrift | drifted = true |

## Algorithm

1. **Load** — Read grammar shard and validate TypeScript version
2. **Match** — Compare code patterns against shard rules
3. **Verify** — Ensure no prohibited patterns (any, enum, @ts-ignore)
4. **Report** — Flag any violations before generation

## Negative Logic (PROHIBITED PATHS)

- MUST NOT: Use `any` without proper narrowing
- MUST NOT: Use `enum` — use `as const` objects with `typeof`
- MUST NOT: Use `@ts-ignore` — use `@ts-expect-error` with description
- MUST NOT: Use `namespace` — use ES modules only

## Edge Cases

- **Strict mode**: Ensure tsconfig enforces strict checking
- **Module resolution**: Use NodeNext for ESM compliance
- **Decorator support**: Stage 3 decorators enabled (no legacy)

## Dependencies

- Grammar: @root/hashes/grammar/typescript/typescript.hash.md
- Parent: @root/hashes/LANGUAGE.md

---

*Generated by FMCF Forensic Guardian — 2026-05-14T16:00:00Z*