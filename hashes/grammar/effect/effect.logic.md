# Logic Blueprint: Grammar Shard — @effect-ts/effect v3.x

## State Machine

| State | Transition | Guard |
|-------|------------|-------|
| GRAMMAR_IDLE | loadShard | grammar.valid |
| GRAMMAR_ACTIVE | applyPattern | pattern.anchored |
| GRAMMAR_VERIFIED | syntaxValid | errors.length = 0 |
| GRAMMAR_DRIFTED | detectDrift | drifted = true |

## Algorithm

1. **Load** — Read grammar shard and validate structure
2. **Match** — Compare code patterns against shard rules
3. **Verify** — Ensure no prohibited patterns or drift
4. **Report** — Flag any violations before generation

## Negative Logic (PROHIBITED PATHS)

- MUST NOT: Generate Effect without using TypeScript grammar shard
- MUST NOT: Use deprecated Effect-TS v1 patterns (sync Effect.run, no Cause)
- MUST NOT: Bypass Layer composition — always use Layer.effect or Layer.succeed
- MUST NOT: Use `any` for error types — always use union `E`

## Edge Cases

- **Missing import**: Effect-TS requires explicit imports — flag if missing
- **Version mismatch**: Check SDK version against grammar shard version
- **Layer circular**: Detect circular dependencies in Layer composition

## Dependencies

- Grammar: @root/hashes/grammar/effect/effect.hash.md
- Parent: @root/hashes/LANGUAGE.md

---

*Generated by FMCF Forensic Guardian — 2026-05-14T16:00:00Z*